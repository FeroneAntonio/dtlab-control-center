# DTLab Control Center API (Fase 1). Installare anche il pacchetto dtlab:
#   pip install -e .            (dalla radice del repo)
#   pip install -r apps/api/requirements-api.txt
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
# Test
httpx>=0.27,<1
pytest>=8.4,<9
